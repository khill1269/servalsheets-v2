import { connectorManager } from '../connectors/connector-manager.js';

/**
 * Resource-layer adapter exposing connector runtime primitives to handlers.
 * Keeps handlers decoupled from the connectors implementation layer.
 */
export { connectorManager };
