/**
 * ServalSheets - Bounded Cache (delegates to @serval/core)
 *
 * Re-exports the platform-agnostic BoundedCache from @serval/core.
 * All existing import paths in serval-sheets continue to work unchanged.
 */
export { BoundedCache, type BoundedCacheOptions } from '@serval/core';
