apiVersion: v1
kind: Service
metadata:
  name: { { include "servalsheets.fullname" . } }
  labels: { { - include "servalsheets.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector: { { - include "servalsheets.selectorLabels" . | nindent 4 } }
