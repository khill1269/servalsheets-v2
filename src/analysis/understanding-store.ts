/**
 * Compatibility re-export.
 *
 * The understanding store now lives in services to preserve service-layer
 * boundaries. Keep this module so existing imports continue to work.
 */
export * from '../services/understanding-store.js';
